apiVersion: v1
kind: Service
metadata:
  name: calibre-web-automated
spec:
  ports:
    - name: http
      port: 8083
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: calibre-web-automated
