apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openobserve
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ .Values.hostname }}`)
      kind: Rule
      services:
        - name: openobserve
          port: 5080
