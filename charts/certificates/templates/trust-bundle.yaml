apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: trust-bundle
spec:
  sources:
    - secret:
        name: "root-cert"
        key: "ca.crt"
  target:
    configMap:
      key: "ca-bundle.crt"

