apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: structure
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  destinations:
    - namespace: argocd
      name: in-cluster
  sourceRepos:
    - {{ .Values.repoURL }}
